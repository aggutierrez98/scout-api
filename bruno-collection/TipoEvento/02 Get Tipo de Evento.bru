meta {
  name: Get Tipo de Evento
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/tipo-evento/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: tipo-evento-uuid-aqui
}
