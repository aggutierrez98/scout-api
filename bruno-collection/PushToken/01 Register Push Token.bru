meta {
  name: Register Push Token
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/push-token
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "platform": "WEB",
    "token": "fcm-token-de-ejemplo-aqui"
  }
}
