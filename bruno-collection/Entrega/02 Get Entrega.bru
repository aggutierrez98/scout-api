meta {
  name: Get Entrega
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/entrega/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:path {
  id: entrega-uuid-aqui
}
